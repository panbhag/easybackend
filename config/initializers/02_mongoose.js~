var mongoose = require('mongoose');
//var db = mongoose.connect('mongodb://localhost/owftest');
var  db = mongoose.connect('mongodb://localhost/owflive');
var mongooseTypes = require("mongoose-types");
mongooseTypes.loadTypes(mongoose);
